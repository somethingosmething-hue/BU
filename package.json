{
  "name": "mimu-clone",
  "version": "1.0.0",
  "description": "A Mimu-inspired Discord bot with web dashboard",
  "scripts": {
    "start": "concurrently \"npm run bot\" \"npm run web\"",
    "bot": "cd bot && npm start",
    "web": "cd website && npm start",
    "dev": "cd bot && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
